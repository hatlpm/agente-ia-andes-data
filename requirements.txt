# --- Orquestacion del agente (LangChain) ---
langchain>=1.3,<2
langchain-core>=1.0,<2
langchain-community>=0.4,<0.5
langchain-text-splitters>=1.0,<2

# --- Proveedor del modelo: Google Gemini (LLM + embeddings) ---
langchain-google-genai>=4.3,<5

# --- Indice vectorial para la busqueda semantica (RAG) ---
faiss-cpu>=1.14,<2

# --- Lectura y analisis de documentos ---
pypdf>=6,<7
pandas>=2.2,<3
tabulate>=0.9,<1

# --- Interfaz web ---
streamlit>=1.40,<2

# --- Configuracion ---
python-dotenv>=1.2,<2

# --- Solo para generar el PDF de datos ficticios (no lo usa la app) ---
reportlab>=4,<5
